<div class="container__profile">
    <h1 class="title__profile">¡Hola, {{user.firstName}} {{user.lastName}}!
    </h1>
    <div class="info__profile">
        <h2 class="subtitle">Datos del perfil:</h2>
        <ul>
            <li>
                <p>Email: {{user.email}}</p>
            </li>
            <li>
                <p>Rol: {{user.rol}}</p>
            </li>
        </ul>
    </div>
    <div>
        {{#if notAdminOptions}}
        <a href="/users/documents">Actualizar Documentación</a>
        {{/if}}
    </div>

    {{#if adminOptions}}
    <h3>Menú de administrador</h3>
    <a href="/users/getUsers">Ver todos los usuarios</a>
    {{/if}}
    <div>
        <a class="back" href="/users"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"
                class="bi bi-arrow-return-left" viewBox="0 0 16 16">
                <path fill-rule="evenodd"
                    d="M14.5 1.5a.5.5 0 0 1 .5.5v4.8a2.5 2.5 0 0 1-2.5 2.5H2.707l3.347 3.346a.5.5 0 0 1-.708.708l-4.2-4.2a.5.5 0 0 1 0-.708l4-4a.5.5 0 1 1 .708.708L2.707 8.3H12.5A1.5 1.5 0 0 0 14 6.8V2a.5.5 0 0 1 .5-.5" />
            </svg>Inicio</a>
    </div>
</div>